/**
 * pages/register.js — Trang đăng ký
 * ─────────────────────────────────────────────────────────────
 * Trách nhiệm:
 *   - Validate form (tên, email, phone, password)
 *   - Preview ảnh đại diện (FileReader → LocalStorage)
 *   - Gọi authService.register()
 *   - Redirect về index.html sau khi tạo tài khoản thành công
 *   - Hiển thị lỗi inline (email trùng, password yếu, v.v.)
 * ─────────────────────────────────────────────────────────────
 */

import { register, isLoggedIn } from '../services/authService.js';

// TODO: function init() { ... }
// TODO: function handleSubmit(e) { ... }
// TODO: function handleAvatarChange(e) { ... }
// TODO: function validateForm(data) { ... }
// TODO: function showError(field, message) { ... }

// document.addEventListener('DOMContentLoaded', init);
