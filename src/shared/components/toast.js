/**
 * toast.js
 * ─────────────────────────────────────────────────────────────
 * Hiển thị toast notification (thông báo ngắn góc màn hình).
 *
 * Cách dùng:
 *   import { toast } from '../../shared/components/toast.js';
 *   toast.success('Đặt ghế thành công!');
 *   toast.error('Ghế đã được người khác chọn.');
 *   toast.info('Còn 1 phút để hoàn tất thanh toán.');
 *
 * Trách nhiệm:
 *   toast.show(message, type, duration) — Hiện toast
 *   toast.success(message)              — Shorthand
 *   toast.error(message)                — Shorthand
 *   toast.info(message)                 — Shorthand
 * ─────────────────────────────────────────────────────────────
 */

// TODO: function _createContainer() { ... } — Tạo #toast-container nếu chưa có
// TODO: function _show(message, type, duration = 3000) { ... }
// TODO: export const toast = { show, success, error, info }
