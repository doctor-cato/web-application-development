/**
 * movieCard.js
 * ─────────────────────────────────────────────────────────────
 * Component tạo HTML card hiển thị thông tin phim.
 *
 * Cách dùng:
 *   import { createMovieCard } from '../../shared/components/movieCard.js';
 *   container.appendChild(createMovieCard(movie));
 *
 * Trách nhiệm:
 *   createMovieCard(movie)      — Trả về DOM element cho 1 phim
 *   createComingSoonCard(movie) — Card phim sắp chiếu (có badge "Sắp chiếu")
 *
 * Props movie object:
 *   { id, title, genre[], rating, duration, poster, ageRating, isNowShowing }
 * ─────────────────────────────────────────────────────────────
 */

// TODO: export function createMovieCard(movie) { ... }
// TODO: export function createComingSoonCard(movie) { ... }
// TODO: function _ratingBadge(rating) { ... }
// TODO: function _genreTags(genres) { ... }
