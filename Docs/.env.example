# Example .env file (DO NOT COMMIT REAL CREDENTIALS)
# Copy this to server/.env and client/.env and replace placeholders for local development only.

# Server
MONGO_URI=REPLACE_WITH_SECRET_STORE_VALUE
JWT_SECRET=REPLACE_WITH_SECRET_STORE_VALUE
PORT=5000

# Client (Vite)
VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
